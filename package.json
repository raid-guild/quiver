{
  "name": "@raidguild/quiver",
  "version": "0.3.0",
  "private": false,
  "scripts": {
    "build": "pnpm run --recursive build",
    "release": "pnpm run --filter quiver build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.17.0",
    "typescript": "^4.4.4"
  }
}
